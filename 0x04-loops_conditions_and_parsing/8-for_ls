#!/usr/bin/env bash
# This script lists files in current directory and prints only the part after the first dash

for file in *
do
  # skip directories or hidden files automatically handled by pattern *
  if [ -f "$file" ]
  then
    echo "$file" | cut -d '-' -f2-
  fi
done
